# ============================================
# CONFIGURACIÓN DE RED
# ============================================
# Reemplaza con tu dirección IP local (obtenida con ipconfig)
# Ejemplo: 192.168.1.22
YOUR_LOCAL_IP=localhost

# ============================================
# BACKEND (Django)
# ============================================
# Puerto del backend (por defecto 8000)
BACKEND_PORT=8000

# URL completa del backend
# Ejemplo: https://192.168.1.22:8000
BACKEND_URL=https://${YOUR_LOCAL_IP}:${BACKEND_PORT}

# ============================================
# FRONTEND (React)
# ============================================
# Puerto del frontend (por defecto 3000)
FRONTEND_PORT=3000

# URL completa del frontend
# Ejemplo: https://192.168.1.22:3000
FRONTEND_URL=https://${YOUR_LOCAL_IP}:${FRONTEND_PORT}

# Habilitar HTTPS en React
HTTPS=true

# Rutas a los certificados SSL (generados con mkcert)
SSL_CRT_FILE=cert.pem
SSL_KEY_FILE=key.pem

# ============================================
# DJANGO SETTINGS (Opcional)
# ============================================
# Descomenta y configura si decides usar variables de entorno en Django

# SECRET_KEY=tu-secret-key-super-secreta-aqui
# DEBUG=True
# ALLOWED_HOSTS=localhost,127.0.0.1,${YOUR_LOCAL_IP}

# ============================================
# CORS SETTINGS (Opcional)
# ============================================
# CORS_ALLOWED_ORIGINS=${FRONTEND_URL}

# ============================================
# DATABASE (Para producción)
# ============================================
# Descomenta si migras a PostgreSQL u otra BD

# DB_ENGINE=django.db.backends.postgresql
# DB_NAME=nombre_base_datos
# DB_USER=usuario
# DB_PASSWORD=contraseña
# DB_HOST=localhost
# DB_PORT=5432

# ============================================
# NOTAS DE USO
# ============================================
# 1. Copia este archivo como .env en los directorios BackEnd y FrontEnd
# 2. Reemplaza YOUR_LOCAL_IP con tu IP local real
# 3. No subas el archivo .env al repositorio (ya está en .gitignore)
# 4. Cada desarrollador debe crear su propio .env con su configuración local
